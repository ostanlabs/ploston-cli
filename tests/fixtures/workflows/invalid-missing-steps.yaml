# Workflow: Invalid - Missing Steps
# This workflow is intentionally invalid for testing validation
name: invalid-missing-steps
version: "1.0"
description: Invalid workflow - missing required steps field

inputs:
  - name: message
    type: string
    required: true

# steps field is intentionally missing to test validation

outputs:
  - name: result
    value: "{{ inputs.message }}"
